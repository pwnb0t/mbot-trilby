<Window x:Class="ownbotsidekick.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:ownbotsidekick.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        FontFamily="Segoe UI Variable Text"
        Title="m'bot Trilby"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowState="Maximized"
        Loaded="Window_Loaded">
    <Grid x:Name="RootOverlayGrid"
          Visibility="{Binding OverlayVisibility}"
          Background="Transparent">
        <Border x:Name="OverlayPanelBorder"
                Margin="0"
                Background="{StaticResource OverlayBackgroundBrush}"
                BorderThickness="0"
                CornerRadius="0"
                Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Vertical">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <Border Background="#263242"
                                    BorderBrush="{StaticResource AccentBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="6,2"
                                    Margin="0,0,10,0">
                                <TextBlock Text="m'bot"
                                           Foreground="{StaticResource AccentBrush}"
                                           FontSize="12"
                                           FontWeight="Bold" />
                            </Border>
                            <TextBlock Text="m'bot Trilby"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                        <UniformGrid Rows="1"
                                     Columns="3"
                                     Width="640"
                                     HorizontalAlignment="Left">
                            <StackPanel Margin="4,0">
                                <TextBlock Text="Alt+1"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,6"
                                           TextAlignment="Center" />
                                <Button x:Name="QuickPlay1Button"
                                        Style="{StaticResource ClipButtonStyle}"
                                        Height="86"
                                        Click="QuickPlay1Button_Click"
                                        AllowDrop="True"
                                        DragEnter="QuickPlayButton_DragEnter"
                                        DragLeave="QuickPlayButton_DragLeave"
                                        DragOver="QuickPlayButton_DragOver"
                                        Drop="QuickPlay1Button_Drop">
                                    <StackPanel>
                                        <TextBlock x:Name="QuickPlay1TriggerTextBlock"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   FontSize="15"
                                                   FontWeight="SemiBold"
                                                   TextAlignment="Center"
                                                   TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                            <StackPanel Margin="4,0">
                                <TextBlock Text="Alt+2"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,6"
                                           TextAlignment="Center" />
                                <Button x:Name="QuickPlay2Button"
                                        Style="{StaticResource ClipButtonStyle}"
                                        Height="86"
                                        Click="QuickPlay2Button_Click"
                                        AllowDrop="True"
                                        DragEnter="QuickPlayButton_DragEnter"
                                        DragLeave="QuickPlayButton_DragLeave"
                                        DragOver="QuickPlayButton_DragOver"
                                        Drop="QuickPlay2Button_Drop">
                                    <StackPanel>
                                        <TextBlock x:Name="QuickPlay2TriggerTextBlock"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   FontSize="15"
                                                   FontWeight="SemiBold"
                                                   TextAlignment="Center"
                                                   TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                            <StackPanel Margin="4,0">
                                <TextBlock Text="Alt+3"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,6"
                                           TextAlignment="Center" />
                                <Button x:Name="QuickPlay3Button"
                                        Style="{StaticResource ClipButtonStyle}"
                                        Height="86"
                                        Click="QuickPlay3Button_Click"
                                        AllowDrop="True"
                                        DragEnter="QuickPlayButton_DragEnter"
                                        DragLeave="QuickPlayButton_DragLeave"
                                        DragOver="QuickPlayButton_DragOver"
                                        Drop="QuickPlay3Button_Drop">
                                    <StackPanel>
                                        <TextBlock x:Name="QuickPlay3TriggerTextBlock"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   FontSize="15"
                                                   FontWeight="SemiBold"
                                                   TextAlignment="Center"
                                                   TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>

                    <Grid Grid.Column="1" VerticalAlignment="Top">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <TextBlock x:Name="ClipCountTextBlock"
                                       Text="{Binding ClipCountText}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="0,0,10,0" />
                            <Button Content="Refresh"
                                    Style="{StaticResource TopBarButtonStyle}"
                                    Click="RefreshClipsButton_Click" />
                            <Button Content="X"
                                    Style="{StaticResource TopBarButtonStyle}"
                                    Width="32"
                                    Height="28"
                                    Padding="0"
                                    Margin="0"
                                    Click="CloseOverlayButton_Click" />
                        </StackPanel>

                        <UniformGrid Grid.Row="1"
                                     Rows="1"
                                     Columns="2"
                                     Width="420"
                                     Height="86"
                                     Margin="0,10,0,0"
                                     HorizontalAlignment="Right">
                            <Button Style="{StaticResource ClipButtonStyle}" Content="Play Random" Click="PlayRandomButton_Click" />
                            <Button Style="{StaticResource ClipButtonStyle}" Content="Stop" Click="StopButton_Click" />
                        </UniformGrid>
                    </Grid>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="320" />
                        <ColumnDefinition Width="320" />
                    </Grid.ColumnDefinitions>

                    <controls:SearchPanelControl x:Name="SearchPanel"
                                                 Grid.Column="0"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"
                                                 SearchQueryDisplay="{Binding SearchQueryDisplay}"
                                                 VisibleClips="{Binding VisibleClips}"
                                                 NoResultsVisible="{Binding NoResultsVisible}"
                                                 Width="640"
                                                 Height="500" />

                    <Border Grid.Column="1"
                            Background="{StaticResource PanelBackgroundBrush}"
                            BorderBrush="{StaticResource PanelBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="12"
                            Margin="12,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Text="{Binding RecentStatsTitle}"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Height="24"
                                       Margin="0,0,0,10" />

                            <StackPanel Grid.Row="1" Margin="0,0,0,10">
                                <TextBlock Text="Filters"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="12"
                                           Height="16"
                                           Margin="0,0,0,4" />
                                <StackPanel Orientation="Horizontal" Height="30">
                                    <Button x:Name="RecentStatsScopeMeButton"
                                            Style="{StaticResource StatsFilterButtonStyle}"
                                            Content="Just me"
                                            Click="RecentStatsScopeMeButton_Click" />
                                    <Button x:Name="RecentStatsScopeEveryoneButton"
                                            Style="{StaticResource StatsFilterButtonStyle}"
                                            Content="Everyone"
                                            Click="RecentStatsScopeEveryoneButton_Click" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Height="30" Margin="0,6,0,0">
                                    <Button x:Name="RecentStatsIncludeRandomButton"
                                            Style="{StaticResource StatsFilterButtonStyle}"
                                            Content="Include random"
                                            Click="RecentStatsIncludeRandomButton_Click" />
                                </StackPanel>
                            </StackPanel>

                            <Grid Grid.Row="2">
                                <TextBlock Text="{Binding RecentStatsStatusText}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="12"
                                           Margin="0,0,0,8" />
                                <Border Height="1"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Top"
                                        Margin="14,0,14,0"
                                        Background="{StaticResource TextSecondaryBrush}" />
                                <ScrollViewer Margin="0,22,0,0"
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding RecentClipStats}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Style="{StaticResource TopClipStatButtonStyle}"
                                                        Tag="{Binding Trigger}"
                                                        Click="RecentClipStatsItemButton_Click">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="*" />
                                                            <RowDefinition Height="Auto" />
                                                        </Grid.RowDefinitions>
                                                        <TextBlock Grid.Row="0"
                                                                   Grid.Column="0"
                                                                   Text="{Binding TriggerDisplay}"
                                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                                   FontSize="14"
                                                                   FontWeight="SemiBold"
                                                                   VerticalAlignment="Center"
                                                                   TextTrimming="CharacterEllipsis" />
                                                        <TextBlock Grid.Row="0"
                                                                   Grid.Column="1"
                                                                   Text="&#x1F500;"
                                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                                   FontSize="12"
                                                                   FontWeight="Normal"
                                                                   HorizontalAlignment="Right"
                                                                   VerticalAlignment="Top"
                                                                   Margin="8,0,0,0">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsRandom}" Value="True">
                                                                            <Setter Property="Visibility" Value="Visible" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <TextBlock Grid.Row="1"
                                                                   Grid.Column="1"
                                                                   Text="{Binding PlayedAgoText}"
                                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                                   FontSize="11"
                                                                   FontWeight="Normal"
                                                                   HorizontalAlignment="Right" />
                                                    </Grid>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>
                        </Grid>
                    </Border>

                    <Border Grid.Column="2"
                            Background="{StaticResource PanelBackgroundBrush}"
                            BorderBrush="{StaticResource PanelBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="10"
                            Padding="12"
                            Margin="12,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Text="{Binding TopStatsTitle}"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Height="24"
                                       Margin="0,0,0,10" />

                            <StackPanel Grid.Row="1" Margin="0,0,0,10">
                                <TextBlock Text="Filters"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="12"
                                           Height="16"
                                           Margin="0,0,0,4" />
                                <StackPanel Orientation="Horizontal" Height="30">
                                    <Button x:Name="TopStatsScopeMeButton"
                                            Style="{StaticResource StatsFilterButtonStyle}"
                                            Content="Just me"
                                            Click="TopStatsScopeMeButton_Click" />
                                    <Button x:Name="TopStatsScopeServerButton"
                                            Style="{StaticResource StatsFilterButtonStyle}"
                                            Content="Everyone"
                                            Click="TopStatsScopeGuildButton_Click" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Height="30" Margin="0,6,0,0">
                                    <Button x:Name="TopStatsDays1Button"
                                            Style="{StaticResource StatsFilterButtonStyle}"
                                            Content="1D"
                                            Click="TopStatsDays1Button_Click" />
                                    <Button x:Name="TopStatsDays7Button"
                                            Style="{StaticResource StatsFilterButtonStyle}"
                                            Content="7D"
                                            Click="TopStatsDays7Button_Click" />
                                    <Button x:Name="TopStatsDays30Button"
                                            Style="{StaticResource StatsFilterButtonStyle}"
                                            Content="30D"
                                            Click="TopStatsDays30Button_Click" />
                                    <Button x:Name="TopStatsDaysAllButton"
                                            Style="{StaticResource StatsFilterButtonStyle}"
                                            Content="All time"
                                            Click="TopStatsDaysAllButton_Click" />
                                </StackPanel>
                            </StackPanel>

                            <Grid Grid.Row="2">
                                <TextBlock Text="{Binding TopStatsStatusText}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="12"
                                           Margin="0,0,0,8" />
                                <Border Height="1"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Top"
                                        Margin="14,0,14,0"
                                        Background="{StaticResource TextSecondaryBrush}" />
                                <ScrollViewer Margin="0,22,0,0"
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding TopClipStats}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Style="{StaticResource TopClipStatButtonStyle}"
                                                        Tag="{Binding Trigger}"
                                                        Click="TopClipStatsItemButton_Click">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="*" />
                                                            <RowDefinition Height="Auto" />
                                                        </Grid.RowDefinitions>
                                                        <TextBlock Grid.Row="0"
                                                                   Text="{Binding Trigger}"
                                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                                   FontSize="14"
                                                                   FontWeight="SemiBold"
                                                                   VerticalAlignment="Center"
                                                                   TextTrimming="CharacterEllipsis" />
                                                        <TextBlock Grid.Row="1"
                                                                   Text="{Binding PlayCountText}"
                                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                                   FontSize="11"
                                                                   FontWeight="Normal"
                                                                   HorizontalAlignment="Right" />
                                                    </Grid>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>

            </Grid>
        </Border>
    </Grid>
</Window>
